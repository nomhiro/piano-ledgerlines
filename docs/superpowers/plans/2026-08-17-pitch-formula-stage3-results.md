# pitch の弁別力測定（段3 フェーズ1）— 測定結果

測定日: 2026-08-17
対象: 設計 §5.1 の合格条件4項目（脚1、§1〜§8）と、実録音1件による分類の実在性（脚2、§9）
Issue: #40（pitch が未検証のため総合スコアが常に `null`）
設計: `docs/superpowers/specs/2026-08-15-restore-performance-scores-design.md` §5.1 / §9.1 / §9.2 / §9.3
計画: `docs/superpowers/plans/2026-08-17-pitch-formula-stage3.md` Task 7（脚1）/ Task 8（脚2）

**この文書は測定の記録であり、値の決定ではない。** `TAU_PITCH` / `W_EXTRA` の確定は
Task 9 が担う（理由は後述の「§5 τ が選べない」）。

**§1〜§8 は脚1（摂動、採譜なし）の記録である。§9 が脚2（実録音の採譜）の記録で、
脚1 が原理的に測れなかった「分類が実在のアーティファクトを除去できるか」を測っている。
結論は §9.4 にあり、脚1 の見通しとは大きく異なる。**

---

## 1. 何を測ったか / 測っていないか

測った: `(TAU_PITCH, W_EXTRA)` の18候補（τ 6点 × W_EXTRA 3点）について、ground truth MIDI に
既知の摂動を与えたときの pitch の応答。設計 §5.1 の4条件を判定した。

**測っていない（測れない）:**

- **extra 分類が実在の採譜アーティファクトを除去できるか。** フェーズ1 は `perturb.py` が
  ground truth MIDI を直接加工するだけで**採譜器を通さない**ため、二重検出・倍音ゴースト・
  スプリアス・ペダル残響という分類対象のアーティファクトが1件も存在しない（設計 §9.3）。
  ここで測れるのは**偽陰性方向だけ**、すなわち「分類器が実際の弾き間違いを `extraNoise` に
  誤分類しないこと」である。
- **rhythm / tempo の指標。** 参照譜のグリッドは摂動元と同じ MIDI のオンセットから作った
  等間隔グリッドであり（設計 §9.2、`gridSource: "midi"`）、音楽的な拍ではない。
  **この harness で rhythm / tempo の数値は意味を持たない。** 判定に使っていない。
- **録音条件不変性**（設計 §5.2）。フェーズ2 に属する。

---

## 2. 実行したコマンド列

コード: `feat/pitch-formula-stage3` の `1ebbe7d`（`feat: derive the poc reference grid from
MIDI so phase 1 needs no audio`）に、本コミットの `poc/scripts/sweep_pitch_tau.py` を加えた状態。

入力は `poc/data/maestro-midi.zip`（MAESTRO v3.0.0 の MIDI zip、約 56 MiB）のみ。
音声・librosa・採譜チェックポイントはいずれも不要。

```bash
cd poc
python scripts/prepare_dataset.py --midi-only   # data/dataset/ を作る（設計 9.2）
python scripts/make_reference.py --midi-only    # out/reference/ を作る（MIDI 由来の等間隔グリッド）
python scripts/perturb.py                       # out/transcribed/ に 5曲 × 12条件 = 60 ファイル
python scripts/sweep_pitch_tau.py               # out/metrics/pitch-sweep.json
```

`make_reference.py` に `--midi-only` を付けないと `piece00.clean.wav` を読もうとして
`LibsndfileError` で落ちる（フェーズ1 は音声を持たない）。

`poc/out/` と `poc/data/` は `poc/.gitignore` で除外されているため、`pitch-sweep.json` は
リポジトリに入らない。**本文書がその内容の記録である。**

**再現性について**: `perturb.py` は摂動ごとに `RNG_SEED = 20260725` で乱数を初期化するので、
摂動 MIDI は決定的である。掃引を一度破棄して上記4コマンドを頭から再実行し、18候補 × 12条件の
216 セル全部で**前回値との最大絶対差 0.0** を確認した（詳細は §7）。

---

## 3. 使用したデータ

MAESTRO v3.0.0 の MIDI から先頭 90 秒を切り出した5曲。すべて `audio: false`（MIDI のみ）。

| piece | 元 MIDI | 窓 [s] | 長さ [s] | 音符数 | ペダル CC 数 | 小節数 | グリッド由来 |
|---|---|---|---|---|---|---|---|
| piece00 | 2004/MIDI-Unprocessed_SMF_02_R1_2004_01-05_ORIG_MID--AUDIO_02_R1_2004_05_Track05_wav.midi | 1.093–91.093 | 90.0 | 753 | 1388 | 45 | midi |
| piece01 | 2004/MIDI-Unprocessed_SMF_02_R1_2004_01-05_ORIG_MID--AUDIO_02_R1_2004_06_Track06_wav.midi | 1.032–91.032 | 90.0 | 362 | 640 | 45 | midi |
| piece02 | 2004/MIDI-Unprocessed_SMF_02_R1_2004_01-05_ORIG_MID--AUDIO_02_R1_2004_08_Track08_wav.midi | 1.023–91.023 | 90.0 | 1131 | 911 | 45 | midi |
| piece03 | 2004/MIDI-Unprocessed_SMF_02_R1_2004_01-05_ORIG_MID--AUDIO_02_R1_2004_10_Track10_wav.midi | 1.058–91.058 | 90.0 | 1782 | 1631 | 45 | midi |
| piece04 | 2004/MIDI-Unprocessed_SMF_05_R1_2004_01_ORIG_MID--AUDIO_05_R1_2004_02_Track02_wav.midi | 0.986–90.986 | 90.0 | 1446 | 632 | 45 | midi |

参照音符 合計 **5474**。全曲 `estimatedTempo` = 120.0 BPM、`beatsPerMeasure` = 4、45 小節。
MAESTRO の MIDI は 120 BPM / 4-4 固定のプレースホルダなので、この 120 は楽譜のテンポではない
（設計 §9.2）。グリッドは MIDI オンセットからの等間隔16分割。

`prepare_dataset.py` のログは piece00 を `notes=754` と表示するが、書き出された
`piece00.ref.mid` と参照譜 JSON はいずれも 753 音である（ログ表示側の1音のずれ。
参照譜・摂動 MIDI・掃引はすべて 753 で一貫しており、測定には影響しない）。

摂動は `perturb.py` の 12 条件（`none` / `drop05` / `drop15` / `add05` / `add15` /
`jitter30` / `jitter80` / `tempo05` / `tempo15` / `flat50` / `flat90` / `nopedal`）。
ファイル名は `pieceNN.p_<condition>.mid` で、掃引側は先頭の `p_` を剥がして設計 §5.1 の
条件名に正規化する（`normalize_condition()`）。

**設計 §5.1 の条件2は「`drop 5%` と `drop 10%`」と書いているが `drop10` は存在しない**
（`drop` は 5% と 15% の2点）。条件2 の趣旨は「摂動率が低いほど点が高い」なので、
率で並べた最小（`drop05`）と最大（`drop15`）を比較して判定した。

---

## 4. 掃引の結果

pitch は5曲の値の単純平均（掃引ハーネスが曲ごとの pitch を条件単位に畳んだもの）。
判定は設計 §5.1 の4条件。

**この平均は音符数で重み付けしていない。** 5曲の音符数は 362〜1782 と約5倍の幅があるため
（§3 の表）、**piece01（362 音）が piece03（1782 音）と同じ重みで効く**。曲ごとの pitch 自体は
`compute` が小節を参照音符数で重み付けした値だが、**曲をまたぐ集約は等重み**である。
判定4条件はいずれも条件間の順序比較なので重み付けの有無で結論は動かないが、
**この表の絶対値を「5曲 5474 音全体の pitch」として引用してはならない。**

- c1 = `none` で pitch ≥ 90
- c2 = `drop05` > `drop15`
- c3 = `add05` < `none`
- c4 = 摂動率で並べて単調非増加（`drop` 系・`add` 系それぞれ）

| τ | W_EXTRA | none | drop05 | drop15 | add05 | add15 | c1 | c2 | c3 | c4 | 判定 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 0.15 | 0.3 | 99.90 | 77.20 | 42.33 | 91.63 | 76.23 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.15 | 0.5 | 99.85 | 77.14 | 42.27 | 87.03 | 64.43 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.15 | 0.7 | 99.80 | 77.10 | 42.22 | 82.96 | 54.90 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.20 | 0.3 | 99.92 | 81.72 | 51.34 | 93.54 | 81.40 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.20 | 0.5 | 99.88 | 81.68 | 51.29 | 89.86 | 71.52 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.20 | 0.7 | 99.84 | 81.64 | 51.24 | 86.51 | 63.14 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.25 | 0.3 | 99.94 | 84.75 | 58.04 | 94.74 | 84.73 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.25 | 0.5 | 99.90 | 84.72 | 58.00 | 91.67 | 76.27 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.25 | 0.7 | 99.87 | 84.68 | 57.95 | 88.84 | 68.88 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.30 | 0.3 | 99.95 | 86.93 | 63.17 | 95.56 | 87.04 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.30 | 0.5 | 99.92 | 86.89 | 63.13 | 92.94 | 79.67 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.30 | 0.7 | 99.89 | 86.86 | 63.10 | 90.47 | 73.09 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.35 | 0.3 | 99.95 | 88.56 | 67.21 | 96.16 | 88.75 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.35 | 0.5 | 99.93 | 88.53 | 67.17 | 93.87 | 82.22 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.35 | 0.7 | 99.90 | 88.50 | 67.14 | 91.70 | 76.30 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.40 | 0.3 | 99.96 | 89.83 | 70.46 | 96.62 | 90.06 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.40 | 0.5 | 99.94 | 89.80 | 70.43 | 94.58 | 84.20 | ✅ | ✅ | ✅ | ✅ | **PASS** |
| 0.40 | 0.7 | 99.91 | 89.78 | 70.40 | 92.64 | 78.83 | ✅ | ✅ | ✅ | ✅ | **PASS** |

### 合格した候補

**18 / 18 すべて**が4条件に合格した。すなわち合格した候補は掃引したグリッド全体である。

```
tau ∈ {0.15, 0.20, 0.25, 0.30, 0.35, 0.40} × W_EXTRA ∈ {0.3, 0.5, 0.7}
```

**これは「どの候補も良い」という結果ではなく、「この4条件では候補を区別できない」という
結果である**（§5）。

### pitch 以外の摂動条件（判定外・参考）

判定には使っていないが、pitch が pitch 以外の摂動にどう反応するかの記録。

| condition | pitch (τ=0.15, W=0.7) | pitch (τ=0.25, W=0.5) |
|---|---|---|
| none | 99.80 | 99.90 |
| jitter30 | 99.61 | 99.74 |
| jitter80 | 98.41 | 98.99 |
| tempo05 | 99.80 | 99.90 |
| tempo15 | 99.80 | 99.90 |
| flat50 | 99.80 | 99.90 |
| flat90 | 99.80 | 99.90 |
| nopedal | 99.80 | 99.90 |

`tempo` / `flat` / `nopedal` は pitch を `none` と完全に一致させる（音高の集合を変えないため）。
`jitter` はわずかに下げる（オンセットのゆらぎでアライメント窓を外れる音が出る）。
**繰り返すが、この harness の rhythm / tempo 指標は意味を持たないため、これらの条件は
リズム系指標の検証には使えない。** 上の表は pitch の直交性の参考値にすぎない。

---

## 5. τ が選べない — 4条件は τ に依存しない

**18通り全部が合格したのは偶然ではなく、条件の作り方の必然である。**

pitch は `100 · exp(−e_pitch / τ)` であり、τ > 0 のもとで `e_pitch` に対して**厳密減少**する。
したがって τ をどう変えても `e_pitch` の順序は pitch の順序にそのまま保存される。

- **条件2・3・4 は順序の比較しかしていない**（`drop05` > `drop15`、`add05` < `none`、単調性）。
  小節単位で見れば `e_pitch` の順序は τ に依存しないので、これらの条件は **τ を一切
  制約しない**。実測でも、6つの τ すべてで条件2〜4 の判定が同一だった（§4 の表）。
  なお集計 pitch は小節ごとの pitch を `refNotes` で重み付け平均した量なので、
  τ 非依存が厳密に言えるのは小節単位である。集計値での順序保存は上記のとおり実測で確認した。
- **条件1 も τ を制約しない。** `none` の `e_pitch` はほぼ 0（後述のとおり誤差は5曲 5474 音で
  missed 0 / extraPlayed 1）なので、どの τ でも pitch は 100 近傍になる。実測 99.80〜99.96。

つまり**フェーズ1 の4条件が検証しているのは τ の値ではなく、分類の向き**である
（弾き間違いを `extraNoise` に飲み込まないこと ＝ 条件3、設計 §5.1 の主眼）。
**フェーズ1 では τ を決められない。** これは設計 §9.3 が事前に述べていたことと一致する。

W_EXTRA についても同様に、4条件は「`add` で下がる」という向きだけを要求しており、
0.3 / 0.5 / 0.7 のどれを選ぶかは決めない。

**`TAU_PITCH` / `W_EXTRA` の確定は脚2（実録音の採譜、設計 §9.3 / Task 8）と
Task 9 が担う。** 実データでの `noiseShare` と pitch の水準が実質的な選択基準になる。

---

## 6. 条件3 の実体 — 分類の内訳

条件3（`add` で pitch が下がる）が成立した理由を、pitch の値ではなく分類の実数で記録する。
以下は τ / W_EXTRA に依存しない量である（アライメントと分類だけで決まる）。5曲の合計。

**再現手順**: `pitch-sweep.json` は `{condition, pitch}` しか持たないため、当初この内訳は
コンテナ内でのアドホック解析でしか得られなかった。`poc/scripts/analyze_pitch_sweep_breakdown.py`
（本コミットで追加）が同じ集計をコミットされた形で再現する。

```
cd poc && python scripts/analyze_pitch_sweep_breakdown.py
```

出力は以下の表と `extraNoiseByReason`（add05: duplicate 2 / harmonic 0 / spurious 11 / reverb 1、
add15: duplicate 4 / harmonic 0 / spurious 51 / reverb 1）に厳密に一致する。

| condition | 参照音符 | matched | missed | unplayed | extraPlayed | extraNoise | 摂動 MIDI の音符数（増減） |
|---|---|---|---|---|---|---|---|
| none | 5474 | 5473 | 0 | 1 | 1 | 0 | 5474 (±0) |
| drop05 | 5474 | 5212 | 261 | 1 | 1 | 0 | 5213 (−261) |
| drop15 | 5474 | 4663 | 808 | 3 | 2 | 0 | 4665 (−809) |
| add05 | 5474 | 5472 | 1 | 1 | 251 | 14 | 5737 (+263) |
| add15 | 5474 | 5472 | 1 | 1 | 753 | 56 | 6281 (+807) |

**`add` で注入した誤打がどれだけ計上されたか:**

「飲まれた率」は `extraNoise / (extraPlayed + extraNoise)`。

| condition | 注入した音符 | extraPlayed に残った | extraNoise に飲まれた | 飲まれた率 |
|---|---|---|---|---|
| add05 | 263 | 251 | 14 | 14/265 = 5.3% |
| add15 | 807 | 753 | 56 | 56/809 = 6.9% |

（extraPlayed + extraNoise が注入数を 2 上回るのは、`none` にもある境界由来の 1 音と、
注入音に押し出されて一致を失った実音 1 音の分。`add` で `missed` が 0 → 1 に増えているのが
後者にあたる。）

**`extraNoise` の理由別内訳:**

| condition | duplicate | harmonic | spurious | reverb |
|---|---|---|---|---|
| add05 | 2 | 0 | 11 | 1 |
| add15 | 4 | 0 | 51 | 1 |

**`harmonic` が両条件で 0 件**であることは重要である。ブリーフが警告していたのは
「隣接半音の誤打（`abs(diff) == 1`）が規則2 のオクターブ判定（`abs(diff) == 12`）に
誤って該当する」バグだが、それは起きていない。

飲まれた分の大半は `spurious`（`duration < 60ms` かつ `velocity < 40`、`align.py` の
`NOISE_SPURIOUS_DURATION_SEC` / `NOISE_SPURIOUS_VELOCITY`）である。`perturb.py` の
`add_notes()` は元の音符の `start` / `end` をそのまま複製し velocity を 10 下げるので、
**元が短くて弱い音の誤打はスプリアス閾値に落ちる**。これは分類規則の設計どおりの挙動で、
バグではない。ただし **5〜7% の弾き間違いは計上されない**という定量的な限界であり、
記録に残す。これらの閾値自体は設計 §4.2 / §9.3 のとおり**暫定値でフェーズ2 で確定する**。

**条件1 が満点にならない分の内訳**（設計 §5.1 条件1 が「参照譜の量子化などの系統誤差」
として認めている分）: 5曲 5474 音のうち piece01 の最終音（pitch 52、`startBeat` 178.6875、
第45小節、est 側 89.336–90.000 秒）1音のみ。DTW のどの run にも覆われず `unplayed` になり、
est 側の対応音が `extraPlayed` に落ちる。90 秒窓の末尾の境界効果である。
これが `none` の pitch を 100 から 0.04〜0.20 点下げている唯一の原因。

---

## 7. 再実行による突き合わせ

先行の測定は掃引スクリプトの最終編集より古い出力だったため、**成果物とコードの対応を
保証するために掃引を破棄して頭から再実行した**（`poc/data/dataset` / `poc/out/reference` /
`poc/out/transcribed` / `poc/out/metrics/pitch-sweep.json` を消してから §2 の4コマンド）。

- 参照譜は完全に再現（音符数 753 / 362 / 1131 / 1782 / 1446、45 小節、120.0 BPM）
- 掃引は 18候補 × 12条件 = 216 セルすべてで**前回値との最大絶対差 0.0**
- 18候補の `passed` も全一致

差分は条件ラベルだけだった。旧 JSON は `rows` に `p_none` / `p_drop05` のような生の
条件名を持っており、正規化を `normalize_condition()` に切り出す前の版の出力であることが
確認できた。**数値は変わっていない。**

掃引の標準出力（再実行分、18行）:

```
tau=0.15 w_extra=0.3 none=99.9 c1=True c2=True c3=True c4=True => PASS
tau=0.15 w_extra=0.5 none=99.846 c1=True c2=True c3=True c4=True => PASS
tau=0.15 w_extra=0.7 none=99.798 c1=True c2=True c3=True c4=True => PASS
tau=0.2 w_extra=0.3 none=99.924 c1=True c2=True c3=True c4=True => PASS
tau=0.2 w_extra=0.5 none=99.88 c1=True c2=True c3=True c4=True => PASS
tau=0.2 w_extra=0.7 none=99.84 c1=True c2=True c3=True c4=True => PASS
tau=0.25 w_extra=0.3 none=99.938 c1=True c2=True c3=True c4=True => PASS
tau=0.25 w_extra=0.5 none=99.9 c1=True c2=True c3=True c4=True => PASS
tau=0.25 w_extra=0.7 none=99.86800000000001 c1=True c2=True c3=True c4=True => PASS
tau=0.3 w_extra=0.3 none=99.94800000000001 c1=True c2=True c3=True c4=True => PASS
tau=0.3 w_extra=0.5 none=99.916 c1=True c2=True c3=True c4=True => PASS
tau=0.3 w_extra=0.7 none=99.886 c1=True c2=True c3=True c4=True => PASS
tau=0.35 w_extra=0.3 none=99.954 c1=True c2=True c3=True c4=True => PASS
tau=0.35 w_extra=0.5 none=99.928 c1=True c2=True c3=True c4=True => PASS
tau=0.35 w_extra=0.7 none=99.9 c1=True c2=True c3=True c4=True => PASS
tau=0.4 w_extra=0.3 none=99.96000000000001 c1=True c2=True c3=True c4=True => PASS
tau=0.4 w_extra=0.5 none=99.936 c1=True c2=True c3=True c4=True => PASS
tau=0.4 w_extra=0.7 none=99.912 c1=True c2=True c3=True c4=True => PASS

18/18 candidate(s) passed all four criteria
NOTE: phase 1 does not exercise transcription, so it cannot show whether the
      extra classifier removes real artifacts (design 9.3). That is leg 2.
```

---

## 8. 結論と次の一手

**フェーズ1（脚1）は通った。** 設計 §5.1 の4条件が18候補すべてで成立し、とくに主眼の条件3
について、注入した隣接半音の誤打の 93〜95% が `extraPlayed` に残り `harmonic` 誤分類は
0 件だった。**extra 分類は注入した誤打の 93〜95% を計上しており、大半を飲み込んでいない。**
残る 5〜7%（`add05` で 14/265、`add15` で 56/809）は大半が `spurious` として飲まれている
（§6:223-224 の内訳: add05 は duplicate 2 / spurious 11 / reverb 1、add15 は
duplicate 4 / spurious 51 / reverb 1）。
**「全く飲み込んでいない」ではない。**

**ただしフェーズ1 は `TAU_PITCH` / `W_EXTRA` を決めない。** 4条件は τ に依存しない順序比較で
構成されており、18通り全部が合格するのは想定どおりである（§5）。

未解決のまま次に渡すもの:

1. **`TAU_PITCH` / `W_EXTRA` の値** → 脚2（Task 8、実録音の採譜）と Task 9
2. **extra 分類が実在の採譜アーティファクトを除去できるか** → 脚2。フェーズ1 には
   アーティファクトが存在しないため原理的に測れない（設計 §9.3）
   → **§9 で測定済み。除去できていない（258 件中 2 件、0.78%）。**
3. **分類閾値（`NOISE_*`）の確定** → フェーズ2（設計 §4.2 / §5.2 条件5）。現在の値は
   実測根拠のない暫定値であり、上記の「誤打 5〜7% がスプリアスに落ちる」もこの暫定値の下での数字
   → **§9.3 で、この暫定値が実録音に対してほぼ機能しないことの内訳が取れている。**
4. **rhythm / tempo の検証と録音条件不変性** → フェーズ2（設計 §5.2）

---

## 9. 脚2 — 実録音1件の採譜による測定

測定日: 2026-08-17
計画: Task 8。設計 §9.3 の脚2。

脚1（§1〜§8）は `perturb.py` が ground truth MIDI を直接加工するだけで採譜器を通さないため、
分類が除去対象とするアーティファクト（二重検出・倍音ゴースト・スプリアス・ペダル残響）が
1件も存在しない。**ここは実在のアーティファクトに対する分類の効果を測る唯一の脚である。**

スクリプト: `poc/scripts/measure_real_take.py`。指標の式は worker の `metrics.compute` を
そのまま使い、候補の切り替えは `metrics_mod.TAU_PITCH` / `metrics_mod.W_EXTRA` の差し替えだけで
行っている（設計 §9.1、式の再実装なし）。成果物は `out/metrics/real-take.json`
（`/out/` は `.gitignore` 済みなので、本節がその内容の記録である）。

### 9.1 実行した手順

採譜には torch と採譜チェックポイント（イメージ同梱）が必要なのでワーカーコンテナ内で実行する。
**Task 1〜3 で `worker/` を変更しているため、測定の前にイメージを再ビルドした。** 再ビルドを
省くと分類ロジックが入っていない古いコードを測ることになる。

```bash
docker compose -f docker-compose.azure-local.yml build worker
```

再ビルド後、イメージ内のコードが作業ツリーと一致することを md5 で確認した（`worker/Dockerfile`
は `WORKDIR /app` の直下へ COPY するので、イメージ内のパスは `/app/ledgerlines_worker`）。

```
# コンテナ内                                  # ホスト
d781c4f1bd38cba5f3c0d19bfc8aef09  align.py    d781c4f1bd38cba5f3c0d19bfc8aef09  align.py
fee20dc3920425564e247944e148daba  metrics.py  fee20dc3920425564e247944e148daba  metrics.py
```

`grep -c classify_extra /app/ledgerlines_worker/align.py` → `2`、`NOISE_ONSET_SEC` などの
4閾値と `extraNoiseByReason` もイメージ内に存在する。

測定本体（PowerShell。Git Bash から実行すると MSYS がコンテナ側の絶対パスを
`C:/Program Files/Git/...` に書き換えて失敗する）:

```powershell
docker compose -f "$repo\docker-compose.azure-local.yml" run --rm --no-deps `
  -v "${repo}\poc:/app/poc" -v "${repo}\.data:/app/.data" -v "${repo}\out:/app/out" `
  --entrypoint python worker /app/poc/scripts/measure_real_take.py `
  --audio /app/.data/audio/take_13f27039896544ed885e/original.webm `
  --reference /app/.data/derived/song_af08490e6abf4afe9647/reference.json
```

`--no-deps` と `--entrypoint python` が必要（compose の `worker` は azurite / cosmos の
healthy を待ち、既定の ENTRYPOINT は `python cloud_worker.py`）。採譜は 33 セグメントで
CPU 数分。**同じ音声で2回通した採譜 MIDI は md5 一致（`0032f31c…`）で、採譜は決定的である。**

### 9.2 使用したデータと採譜の結果

対応関係は `.data/takes/take_13f27039896544ed885e.json` の `songId` で確認した。

| 項目 | 値 |
|---|---|
| take | `take_13f27039896544ed885e`（実録音、`audio/webm;codecs=opus`） |
| song | `song_af08490e6abf4afe9647` — `summer-jiu-shi-rang`、48 小節、4/4、D |
| 録音の長さ | 168.9 秒 |
| 参照譜 | `.data/derived/song_af08490e6abf4afe9647/reference.json`、**1121 音** |

**「録音の長さ」は `preprocess` が返す `durationSec` である。これは無音トリム前の値**
（`preprocess.py:124` で `decoded.wav` から算出し、トリムは `:130`）。実測すると
`decoded.wav` = 168.90 秒 / `preprocessed.wav` = **167.00 秒** で、採譜に掛かったのは後者。
「前処理後の長さ」ではないので注意。

`reference.json` は `pedalIntervalsBeats` キーを持つが**中身は空**（0 区間）。したがって
この録音では pedal 指標の参照が無い。extra 分類の判定4（残響）が使うのは採譜側の
`est_pedal` なので、そちらは 158 区間あり判定は動く条件が揃っている。

| 量 | 値 |
|---|---|
| `referenceNotes` | 1121 |
| `transcribedNotes` | 1401 |
| `matchedNotes` | 1015（参照の 90.5%） |
| `missedNotes` | 89 |
| `unplayedNotes` | 17 |
| `retakeNotes` | 128 |
| `takes` | 3 |
| `extraNotes` | 258 |

内訳は両側で閉じている: 参照 1121 = 1015 + 89 + 17、採譜 1401 = 1015 + 258 + 128。
`missed`（DTW に覆われた区間の未一致）と `unplayed`（どの run にも覆われなかった参照音符）、
および `retake`（同じ参照音符に後の run が別の est を割り当てた分の旧 est。`extra` から除かれる）
は `align()` が別のキーで返す（`align.py:271-278`）。**この3つを出さないと内訳が監査できない**
ので `measure_real_take.py` の出力に含めた。

### 9.3 分類の内訳と `noiseShare` — **0.78%。除去できていない**

| 量 | 値 |
|---|---|
| `extraNotes` | 258 |
| `extraNoiseNotes` | **2** |
| `extraPlayedNotes` | 256 |
| **`noiseShare`** | **2 / 258 = 0.0078（0.78%）** |

`extraNoise` の理由別内訳:

| duplicate | harmonic | spurious | reverb |
|---|---|---|---|
| 0 | 0 | **2** | 0 |

`noiseShare` はアライメントと分類だけで決まるので **τ / W_EXTRA に依存しない**。18候補すべてで
0.0078 である。

**分類が pitch に与えた効果は 0.05〜0.09 点**（258 件全部を計上した場合との差。τ=0.15/W=0.7 で
30.24 対 30.16）。実質ゼロである。

**なぜ効かないのか**（`align.py` の4規則を extra 258 件に対して個別に当てた実測。当初はコンテナ内
でのアドホック解析だったが、`poc/scripts/analyze_real_take_extra.py`（本コミットで追加）が
`measure_real_take.py` と同じ採譜済み MIDI・reference.json からコミットされた形で再現する。
下記の候補数・velocity 比・閾値ごとの発火数・同音高オンセット対の統計は、いずれもこのスクリプトの
出力と厳密に一致することを確認済み）:

| 規則 | 前段の条件に該当 | 実際に分類された | 落ちた理由 |
|---|---|---|---|
| 1 duplicate | matched と同音高が ±50ms 内: **0** | 0 | **構造的に不可能**（採譜器が同音高の二重検出を出さない） |
| 2 harmonic | ±50ms 内にオクターブ違いの matched: **69** | 0 | **velocity 比が 0.50 を僅かに超える**（最小 0.60） |
| 3 spurious | `duration < 60ms`: 65 / `velocity < 40`: 15 | **2** | **AND なので同時に満たすのが 2 件だけ** |
| 4 reverb | ペダル区間内に同音高の先行 matched: **9** | 0 | **velocity 比が 0.50 を 0.01 差で外す**（最小 0.51） |

**規則1 と、規則2・4 の落ち方は本質的に違う。** 前者は閾値をどう動かしても捕まらない。
後者は閾値が僅かに厳しいだけである。この区別が閾値見直し（フェーズ2）の出発点になる。

**規則1 — 構造的に不可能。** extra 258 件のうち 225 件（87%）は ±50ms 内に matched 音符を
持つのに、同音高のものは1件も無い。採譜 MIDI 全体で見ると、**同音高の連続オンセット対は
1353 組あり、その onset 差の最小値は 0.1237 秒**（規則1 の窓 50ms の約 2.5 倍）。
`< 100ms` の組も **0 組**である。`piano_transcription_inference` は同一音高について note-off を
経ずに次の note-on を出さないため、この採譜器では「同音高の二重検出」が原理的に発生しない。
**規則1 は閾値をどれだけ緩めてもこの採譜器に対しては発火しない。**
（`retake` として `extra` から除かれた 128 件も規則1 該当は 0 件で、「二重検出が retake に
吸収されて隠れている」わけではない。）

**規則2 — 閾値が僅かに厳しい。** 前段（±50ms 内にオクターブ違いの matched）に **69 件**が
該当する。各件について最も条件に近い matched を取った velocity 比の分布:

| min | p10 | median | max |
|---|---|---|---|
| **0.60** | 0.64 | 0.87 | 1.23 |

閾値を動かしたときの発火数:

| `NOISE_VELOCITY_RATIO` | 0.50（現行） | 0.60 | 0.70 | 0.80 | 0.90 |
|---|---|---|---|---|---|
| 発火数 | **0** | 1 | **9** | **21** | **43** |

**「0.50 で 0 件」は 0.60 未満が1件も無いためである。** 0.70 まで緩めれば 9 件、0.90 なら 43 件が
該当する。一方 max が 1.23 であることから、**比を上げるだけでは 69 件全部は捕まらない**
（オクターブ隣接の matched より velocity が大きい extra が存在する）。

**規則4 — 0.01 差で外している。** ペダル区間（158 区間）内に同音高の先行 matched を持つ extra が
**9 件**あり、velocity 比は **min 0.51 / median 0.86 / max 1.16**。現行の 0.50 では 0 件、
0.60 で 1 件、0.70 で 2 件、0.90 で 6 件。**規則4 は「ペダルデータが無い」のでも
「候補が無い」のでもなく、閾値を 0.01 だけ外して空振りしている。**
（規則1〜3 で分類済みの 2 件を除いた「規則4 に到達する」母集団で数えても同じ 9 件だった。）

**規則3 — AND が効きすぎている。** 片方だけなら 65 件 / 15 件が該当するのに、同時に満たすのは
2 件。extra の velocity は p10/p50/p90 = 44/60/82（最小 21）で、`velocity < 40` に落ちるものが
そもそも 15 件しかない。

**共通の原因は `NOISE_VELOCITY_RATIO = 0.50` が採譜器の velocity 分布に対して厳しすぎること**で、
規則2 と規則4 はどちらもこの比を必須条件に持つため両方が空振りしている。

いずれも**閾値が暫定値である**ことの帰結で（`align.py:302-307`、設計 §4.2 / §9.3）、実測根拠を
まだ持たない値が実データに対して機能していないことが判明したという位置づけである。
**閾値の確定はフェーズ2 の仕事であり、このタスクでは変更していない。** 上の「閾値を動かすと
何件になるか」は分布の位置を記録したものであって、**この録音1件から閾値を決めてはならない**
（§9.6）。

**「閾値を緩めれば `noiseShare` は上がるのでは」への答え — 上限でも 0.20 で、0.50 には届かない。**
この録音で規則2 と規則4 の両方を `NOISE_VELOCITY_RATIO = 0.90` まで緩めたと仮定しても、
分類されるのは規則3 の 2 件 + 規則2 の 43 件 + 規則4 の 6 件 = 51 件で、

    noiseShare の上限 = (2 + 43 + 6) / 258 ≈ 0.20

**規則1 は構造的に 0 のまま**（上記のとおり閾値をどう動かしても発火しない）なので、この 0.20 が
天井である。さらに規則2 の velocity 比の max は 1.23、規則4 は 1.16 なので、**比をいくら上げても
前段該当の全件（69 件 / 9 件）は捕まらない。** つまり **`noiseShare ≥ 0.50` は閾値の緩和だけでは
達成できない。** これは「0.90 まで緩めるべき」という主張ではなく、緩和方向の天井の記録である。

### 9.4 τ × W_EXTRA 18通りの pitch

| τ \ W_EXTRA | 0.3 | 0.5 | 0.7 |
|---|---|---|---|
| 0.15 | 44.89 | 36.47 | **30.24** |
| 0.20 | 53.51 | 45.41 | 39.00 |
| 0.25 | 59.84 | 52.23 | 45.97 |
| 0.30 | 64.67 | 57.57 | 51.58 |
| 0.35 | 68.46 | 61.84 | 56.15 |
| 0.40 | 71.51 | 65.33 | **59.94** |

（太字は現行の出荷値 τ=0.15 / W_EXTRA=0.7 と、τ の上端 0.40 / W_EXTRA=0.7。）

**18通りすべてが 20 < pitch < 95 に収まる**（最小 30.24、最大 71.51）。床にも飽和にも
張り付いていない。

**Task 9 の決定規則との関係（重要）:** Task 9 は「`noiseShare ≥ 0.50` かつ `20 < pitch < 95`
を満たす候補のうち τ 最小」を選ぶ規則だが、`noiseShare` は 0.0078 で **18候補すべてが第1条件を
落とす**。`noiseShare` は τ に依存しないので、候補を変えてもこれは動かない。
**この録音では Task 9 の決定規則をそのままの形で適用できない。** 第2条件だけは18候補すべてが
満たしている。判断は Task 9 に委ねる（この文書は値を決めない）。

### 9.5 `issue8_take_diagnostic.json` との比較

| 量 | issue8 診断 | 本測定 |
|---|---|---|
| song / take | `song_9f548aff…` / `take_980da1b9…` | `song_af08490e…` / `take_13f27039…` |
| `referenceNotes` | 1242 | 1121 |
| `transcribedNotes` | 1495 | 1401 |
| `matchedNotes` | 974（78.4%） | 1015（**90.5%**） |
| `missedNotes` | 268（**派生値**、下記） | 89 |
| `extraNotes` | **521** | **258** |
| pitch（τ=0.15 / W_EXTRA=0.7） | **9.99** | **30.24** |

**issue8 の 268 は fixture に実在するキーではない。** `issue8_take_diagnostic.json` の
キーは `songId` / `takeId` / `referenceNotes` / `transcribedNotes` / `matchedNotes` /
`extraNotes` / `rawScores` / `expected` で、`missedNotes` も `unplayed` も持たない。268 は 1242 − 974 の派生値であり、
その take の `unplayed` が 0 とは限らないので、**`missed` としては上限値である**
（本測定では 1121 − 1015 = 106 のうち 89 が `missed`、17 が `unplayed` だった）。

規模（参照音符 1121 対 1242、採譜 1401 対 1495）は同じ桁で、比較可能である。違うのは
**アライメントの質**で、matched が 78.4% → 90.5%、extra が 521 → 258 と半減している。

**この差はコード改善によるものではない。** `git diff f85a4fa..HEAD -- align.py` の削除行は
`extra` をローカル変数に括り出したリファクタ 6 行だけで、jump DTW・`retakes`・`unplayed` は
issue8 診断が採られた M5 時点（`f85a4fa`）から既に存在する。段3 の2コミット
（`2af54f3` / `eaf718d`）が足したのは分類とペダル配線だけであり、その分類の寄与は §9.3 の
とおり 0.05〜0.09 点でしかない。**つまり 9.99 → 30.24 は録音（曲・演奏の一致度）の違いである。**

**設計 §2.2 の「extra を全て除去しても 24 点にしかならず `TAU_PITCH = 0.15` が支配的」という
分析は、この録音にはそのまま当てはまらない。** §2.2 の 24 点は issue8 の take の
`missed / ref` = 268/1242 = 21.6% から出た値である（τ=0.15 で `exp(−0.216/0.15)` ≈ 0.24）。
この 21.6% は上記のとおり**上限値**（真の `missed` はこれ以下）なので、§2.2 の 24 点も
下限寄りの見積りである。以下の結論の向きは変わらない。
本測定の take は `missed / ref` = 89/1121 = **7.9%** で、extra を1件も計上しない極限の pitch を
実測すると τ=0.15 で **65.13**（τ=0.20 で 71.32、τ=0.40 で 83.29）である。

したがってこの録音で pitch を 30.24 まで押し下げているのは τ ではなく、**計上された
256 件の `extraPlayed`** である（65.13 → 30.24 の 35 点分）。分類がこの 256 件をどれだけ
正しく「本当の弾き間違い」と判定できているかは本測定では検証していない（参照する ground truth
が無い）。**§9.3 の結論は「分類が実在のアーティファクトを除去できていない」ことであって、
「256 件が全て本物の弾き間違いである」ことではない。**

なお、極限の pitch を `exp(−(missed/ref)/τ)` で手計算すると 59 前後になるが、実際の
`compute` は小節ごとに `decay` を取ってから参照音符数で重み付き平均するので一致しない
（上の 65.13 が実測値）。**この文書の数値はすべて `metrics.compute` の出力である。**

### 9.6 この測定の限界

- **1件の録音では分布を語れない**（設計 §9.3）。曲は1曲、演奏者は1人、録音環境は1つ、
  採譜器の設定は1通りである。**用途は τ 候補の裏付けに限る。**
- とくに `noiseShare = 0.0078` を「分類は常に効かない」と一般化してはならない。言えるのは
  「この録音では効かなかった」ことと、§9.3 の**規則ごとの落ち方（velocity 比 0.50 が
  実測 velocity 分布に対して厳しすぎる / 規則3 の AND / 規則1 が採譜器の性質上空振りする）は
  1件でも機構として読み取れる**、という2点までである。閾値の確定には設計 §5.2 条件5 が言う
  clean / room の extra 特徴分布（フェーズ2）が必要である。
- `pedalIntervalsBeats` が空なので、参照側ペダルを伴う録音での挙動は測れていない。
- 対象の take は `.data` 上では `status: "failed"`（`failure.code: INTERNAL`）である。
  本測定はワーカーのジョブ実行を経ず、音声と参照譜から採譜・照合・採点を直接やり直しており、
  この失敗履歴は測定に影響しない。

---

## 10. 結論（Task 9）— τ を確定しない

Task 9 の決定規則（ブリーフ「決定規則」1〜4）は、脚1 で4条件全部に合格した18候補を、
脚2 の実測で絞り込むという手順だった。実測の結果、**この規則を規則が想定した形で適用できず、
τ / W_EXTRA を確定しない（選択肢 A を採る）。**

**確定しなかった理由:**

1. **決定規則の第1条件（`noiseShare ≥ 0.50`）を、18候補すべてが落ちる。** 実測の
   `noiseShare` は 0.78%（§9.3）で、これは `noiseShare` がアライメントと分類だけで決まり
   τ / W_EXTRA に依存しないため、候補をどう変えても動かない（§9.4）。閾値
   `NOISE_VELOCITY_RATIO` を現行の 0.50 から 0.90 まで緩めたと仮定しても上限は約 0.20 で、
   0.50 には届かない（§9.3 末尾）。したがって決定規則が定める「実データ根拠のある候補への
   絞り込み」が実行できない。
2. **決定規則の第4項（全滅時の代替: 1 の合格候補のうち τ 最小を選ぶ）を採らない。** ブリーフの
   この項は「`noiseShare` と実録音 pitch を明記して『実データ根拠が弱い』と残す」ことを
   条件に τ の確定を許すが、コントローラの裁定（Task 9 dispatch）はこれを上書きし、
   **根拠の無い数字をユーザーに見せない**という #29 以来の原則を優先して選択肢 A
   （保留の継続）を採ると決めた。18候補全部が脚1 に合格しているため「1 の合格候補」は
   18通りあり、そこから τ 最小（0.15）を選ぶことは可能だが、その選択を裏付けるのは
   「脚1 は τ を区別しない」ことと「脚2 は分類が効かないこと」の2つの否定的な結果だけで、
   30.24〜71.51 点という pitch の値自体を裏付ける実データは無い（§9.4）。
3. **フェーズ1 は τ を原理的に選べない。** `pitch = 100*exp(-e_pitch/τ)` は τ > 0 で
   `e_pitch` に対して厳密減少するため、順序比較でできている5.1 の4条件は τ の値に
   依存しない（§5）。これは「たまたま18/18が合格した」のではなく、条件の作り方から
   導かれる必然であり、フェーズ1 をどれだけ広い候補グリッドで再実行しても解消しない。

**したがって本文書は τ / W_EXTRA の確定値を提示しない。** `worker/ledgerlines_worker/metrics.py`
の `TAU_PITCH` / `W_EXTRA` は 0.15 / 0.7 のまま変更せず、`confidence.py` の
`decide("pitch")` は `withheld` / `PITCH_FORMULA_UNVALIDATED` を返し続け、`overallScore` は
`null` のままである。**Issue #40 はこの段では閉じない。**

**次に必要なもの**: フェーズ2（設計 §5.2、MAESTRO 音声による clean/room の extra 特徴分布の
実測）で分類閾値（`NOISE_*` の4定数）を実データから確定する。閾値が実測の velocity 分布に
合う値へ更新されない限り、`noiseShare` は上がらず、Task 9 の決定規則を意図どおりに
適用できる候補は出てこない（§9.3・§9.6）。教師較正（τ の音楽的妥当性の検証）は本測定の
範囲外のまま残る（設計 §4.3 / §8 / §9.4）。

# PDF の OMR をキュー経由で実行する Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** デプロイ環境（azure バックエンド）で PDF をアップロードしたとき、`omr-jobs` キュー経由でワーカーが Audiveris を実行し、曲を `reviewing_score` または `omr_failed` に終端させる。

**Architecture:** #33 が参照譜生成に使った形をそのまま写す。純ロジックを `worker/ledgerlines_worker/omr_job.py` に置き（Azure SDK 非依存）、`cloud_worker.py` がキュー受信とストア実装を担う。OMR のアルゴリズムは既存の `worker_main.py:run_omr` を呼ぶだけで再実装しない。Web 側は `getScoreQueue()` と同じ形の `getOmrQueue()` を足し、PDF 分岐で enqueue する。

**Tech Stack:** Python 3.11（unittest、Azure SDK は `cloud_worker.py` に閉じる）、TypeScript / Next.js 16 App Router、`@azure/storage-queue`、Azurite、Bicep、Audiveris 5.10.2（AGPL-3.0）、Docker

**Spec:** `docs/superpowers/specs/2026-08-17-omr-queue-design.md`

## Global Constraints

- **OMR のアルゴリズムを再実装しない。** `worker_main.py` の `run_omr(data_dir, song_id)` をそのまま呼ぶ（設計 §4.3）
- **`omr_job.py` は `azure.*` を import しない。** ワーカーイメージ外で単体テストが動くこと（`score_job.py` と同じ方針）
- **キュー名は未設定・空文字で既定値 `omr-jobs` にフォールバックする。** `required()` にしてはならない。CD は `az containerapp update --image` だけを行い Bicep を流さないため、ワーカーが再起動ループに入り動いている解析パイプラインまで落とす（#33 で踏んだ Critical と同型）
- **失敗時の終端は `omr_failed` ＋ `omrError`。** `awaiting_score` ではない（`docs/spec/api.md:278`）
- **プレビューや変換後の楽譜のファイル名を決め打ちしない。** `run_omr` が書いた曲 JSON から読む
- **測れていない値を検証済みとして書かない。** 実際の楽譜 PDF が無いため成功経路は未検証のまま残る（設計 §5.2）。結果にそう書く
- 既存テストの期待値を、実装を通すために書き換えない
- 未使用 import を残さない
- worker のテストは `unittest`（pytest は使わない）。TS の単体テストは `node:test` ＋ `tsx --test` で、`package.json` の `test:unit` に登録する

---

## File Structure

| ファイル | 責務 |
|---|---|
| `worker/Dockerfile` | Audiveris 5.10.2 ＋ headless JRE の同梱 |
| `worker/README.md` | Audiveris のバージョン・AGPL-3.0・ソース入手元・ライセンス本文の置き場所 |
| `worker/ledgerlines_worker/omr_job.py`（新規） | OMR ジョブの純ロジック（状態遷移・試行上限・アップロード対象の決定） |
| `worker/tests/test_omr_job.py`（新規） | 上記の分岐をフェイクストアで固定 |
| `worker/cloud_worker.py` | `omr_queue_name()`、`_drain_omr_queue`、`_drain_analysis_queue` の抽出、優先順位 |
| `worker/tests/test_cloud_worker_queues.py`（新規） | キュー名のフォールバックとループ優先順位 |
| `src/lib/server/queue.ts` | `OmrJob` / `OmrQueue` / `LocalOmrQueue` / `AzureOmrQueue` / `getOmrQueue` |
| `src/lib/server/config.ts` | `omrQueueName` |
| `src/app/api/songs/[songId]/score/route.ts` | PDF 分岐で azure バックエンドなら enqueue |
| `src/app/songs/[id]/page.tsx` | `converting_score` で差し替え導線を出す |
| `src/components/VerifiedScoreReplacement.tsx` | 上記に対応する理由の表示 |
| `infra/main.bicep` | `omr-jobs` キュー |
| `docs/operations/iac-runbook.md` | §5.1 に適用順序 |
| `docs/spec/api.md` | status 表の「承認するまで待機」を本文と一致させる |

---

### Task 1: Audiveris をワーカーイメージに同梱する

**Files:**
- Modify: `worker/Dockerfile`（`ffmpeg libsndfile1` を入れている apt レイヤーの直後）
- Modify: `worker/README.md:150-156` 付近

**Interfaces:**
- Produces: イメージ内で環境変数 `AUDIVERIS_COMMAND` の既定値 `audiveris` が解決できること。`run_omr` は `AUDIVERIS_COMMAND` が実ファイルならそれ単体、そうでなければ `shlex.split` してコマンド列にする（`worker_main.py:217-218`）

- [ ] **Step 1: 現状を確認する（Audiveris が入っていないことを目で見る）**

```bash
docker build -f worker/Dockerfile -t ledgerlines-worker:omr-before .
docker run --rm --entrypoint sh ledgerlines-worker:omr-before -c "command -v audiveris || echo 'audiveris: not found'"
```

Expected: `audiveris: not found`

**注意**: このビルドは採譜チェックポイント（約164MiB）を取得するため数分かかる。以降のビルドはレイヤーキャッシュが効く。

- [ ] **Step 2: Dockerfile に JRE と Audiveris を追加する**

`RUN apt-get update ... ffmpeg libsndfile1 ...` のレイヤーの**直後**に次を足す。既存の apt レイヤーは変更しない（変更すると torch 等の後続レイヤーのキャッシュが失われる）。

```dockerfile
# PDF の OMR（Audiveris）。デプロイ環境でも PDF を MusicXML 化するために同梱する
# （Issue #45）。ワーカー本体は Python だが Audiveris は JVM アプリなので
# headless JRE も入れる。
#
# ライセンス: Audiveris は AGPL-3.0。バージョン・入手元・ライセンス本文の
# 置き場所は worker/README.md に記録している。
ARG AUDIVERIS_VERSION=5.10.2
ENV AUDIVERIS_HOME=/opt/audiveris \
    AUDIVERIS_COMMAND=/opt/audiveris/bin/Audiveris
RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        openjdk-17-jre-headless tesseract-ocr tesseract-ocr-eng unzip curl \
    && rm -rf /var/lib/apt/lists/*
COPY worker/scripts/fetch_audiveris.py /tmp/fetch_audiveris.py
RUN python /tmp/fetch_audiveris.py && rm /tmp/fetch_audiveris.py
```

- [ ] **Step 3: 取得スクリプトを書く**

Create `worker/scripts/fetch_audiveris.py`。**`fetch_checkpoint.py` と同じ方針**（標準ライブラリのみ、再試行あり、検証あり）。`fetch_checkpoint.py` の `is_retryable` / `fetch_with_retry` を再利用せず複製してはならない——同じディレクトリなので import できる。

```python
"""ビルド時に Audiveris（AGPL-3.0）を取得して /opt/audiveris へ展開する。

`worker/Dockerfile` の専用 RUN レイヤーから requirements.txt のインストールより
前に実行されるため、Python標準ライブラリだけで完結させる。

取得の再試行は fetch_checkpoint.py の実装を再利用する（同じ配布元由来の一過性の
失敗でビルドを落とさないため。Issue #12 で CD が Zenodo の 504 で落ちている）。
"""

from __future__ import annotations

import os
import pathlib
import shutil
import sys
import zipfile

from fetch_checkpoint import fetch_with_retry

VERSION = os.environ.get("AUDIVERIS_VERSION", "5.10.2")
URL = (
    f"https://github.com/Audiveris/audiveris/releases/download/"
    f"{VERSION}/Audiveris-{VERSION}-linux-x86_64.zip"
)
DEST_DIR = pathlib.Path(os.environ.get("AUDIVERIS_HOME", "/opt/audiveris"))
ARCHIVE = pathlib.Path("/tmp/audiveris.zip")
# 実体は数十MB。HTMLのエラー/リダイレクトページを弾くための下限。
MIN_SIZE_BYTES = 10 * 1024 * 1024


def verify(path: pathlib.Path) -> str | None:
    size = path.stat().st_size
    if size < MIN_SIZE_BYTES:
        return (
            f"size {size} bytes is below the {MIN_SIZE_BYTES} floor - "
            "likely an HTML error/redirect page, not the Audiveris archive"
        )
    if not zipfile.is_zipfile(path):
        return "downloaded file is not a zip archive"
    return None


def main() -> int:
    print(f"downloading Audiveris {VERSION} from {URL}")
    problem = fetch_with_retry(URL, ARCHIVE, verify=verify)
    if problem is not None:
        print(problem, file=sys.stderr)
        return 1

    DEST_DIR.mkdir(parents=True, exist_ok=True)
    with zipfile.ZipFile(ARCHIVE) as archive:
        archive.extractall(DEST_DIR)
    ARCHIVE.unlink()

    # 配布アーカイブが1階層のディレクトリに包まれている場合は中身を持ち上げて、
    # AUDIVERIS_COMMAND のパスがバージョンに依存しないようにする。
    entries = [p for p in DEST_DIR.iterdir()]
    if len(entries) == 1 and entries[0].is_dir():
        inner = entries[0]
        for item in list(inner.iterdir()):
            shutil.move(str(item), str(DEST_DIR / item.name))
        inner.rmdir()

    launcher = DEST_DIR / "bin" / "Audiveris"
    if not launcher.exists():
        print(
            f"launcher not found at {launcher}; archive layout differs. "
            f"contents: {sorted(p.name for p in DEST_DIR.iterdir())}",
            file=sys.stderr,
        )
        return 1
    launcher.chmod(0o755)
    print(f"Audiveris {VERSION} installed at {DEST_DIR}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: ビルドして Audiveris が動くことを確認する**

```bash
docker build -f worker/Dockerfile -t ledgerlines-worker:omr .
docker run --rm --entrypoint sh ledgerlines-worker:omr -c '"$AUDIVERIS_COMMAND" -help; echo "exit=$?"'
```

Expected: Audiveris の使用方法が表示され、プロセスが起動したことが分かる（終了コードは Audiveris の `-help` の実装次第で 0 以外もあり得る。**判定は「起動して使用方法が出る」こと**。`command not found` や `NoClassDefFoundError` は失敗）。

**このステップで配布アーカイブの URL・ファイル名・レイアウトが想定と違って失敗した場合**: 実際に取得できる形に `fetch_audiveris.py` を合わせ、**何をどう変えたかを報告に書く**。想定と違ったこと自体は問題ではない。黙って別バージョンに落とすことだけを避ける。

- [ ] **Step 5: イメージサイズの増分を記録する**

```bash
docker images ledgerlines-worker --format '{{.Tag}} {{.Size}}'
```

`omr-before` と `omr` の差を報告に書く。設計 §3 が「数百MB増える」と述べている前提の実測値になる。

- [ ] **Step 6: README にライセンスを記録する**

`worker/README.md` の Audiveris に触れている箇所（`:153` 付近）に次を追記する。

```markdown
### 同梱している Audiveris のライセンス

ワーカーイメージには **Audiveris 5.10.2** を同梱しています（`/opt/audiveris`、
`AUDIVERIS_COMMAND` が既定で `/opt/audiveris/bin/Audiveris` を指します）。

- ライセンス: **AGPL-3.0**
- 入手元: https://github.com/Audiveris/audiveris/releases/tag/5.10.2
- ソース: https://github.com/Audiveris/audiveris （上記タグ）
- ライセンス本文: 配布アーカイブに含まれるものがイメージ内 `/opt/audiveris` 配下に
  そのまま展開されています

このイメージは GHCR で公開され、ネットワーク越しにサービスを提供します。
**AGPL-3.0 §13（ネットワーク利用時のソース提供義務）が適用されるかの判断と対応は、
配備の責任者が行ってください。**
```

- [ ] **Step 7: Commit**

```bash
git add worker/Dockerfile worker/scripts/fetch_audiveris.py worker/README.md
git commit -m "build: bundle Audiveris in the worker image so deployed OMR can run"
```

---

### Task 2: OMR ジョブの純ロジック

**Files:**
- Create: `worker/ledgerlines_worker/omr_job.py`
- Create: `worker/tests/test_omr_job.py`

**Interfaces:**
- Consumes: `worker_main.run_omr(data_dir: Path, song_id: str) -> int`（Task 2 では引数で受け取るので import しない）
- Produces:
  - `MAX_ATTEMPTS = 3`
  - `process_omr_job(store, job: dict, dequeue_count: int, work_dir: Path, run_omr: Callable[[Path, str], int]) -> str`
  - 戻り値は `"completed"` / `"failed"` / `"skipped"` / `"exhausted"`（いずれも呼び出し側がメッセージを削除してよい）。再配信させたい失敗は例外で送出する
  - `OmrStore` Protocol: `get_song` / `update_song` / `download_score` / `upload_preview`（`score_job.ScoreStore` と同じシグネチャ。`upload_reference` は使わない）

- [ ] **Step 1: 失敗するテストを書く**

Create `worker/tests/test_omr_job.py`:

```python
from __future__ import annotations

import json
import sys
import tempfile
import unittest
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT / "worker"))

from ledgerlines_worker.omr_job import MAX_ATTEMPTS, process_omr_job  # noqa: E402


class FakeStore:
    """`converting_score` の曲1件を持つストア。呼ばれた操作を記録する。"""

    def __init__(self, song: dict | None = None):
        self.song = song
        self.patches: list[dict] = []
        self.uploaded: list[str] = []
        self.raise_on_update = False

    def get_song(self, song_id: str, user_id: str):
        return self.song

    def update_song(self, song_id: str, user_id: str, patch: dict):
        if self.raise_on_update:
            raise RuntimeError("cosmos unavailable")
        self.patches.append(patch)
        if self.song is not None:
            self.song.update(patch)
        return self.song

    def download_score(self, song: dict, target_dir: Path) -> Path:
        target = target_dir / "score.pdf"
        target.write_bytes(b"%PDF-1.4 fake")
        return target

    def upload_preview(self, song: dict, file_name: str, source: Path) -> None:
        self.uploaded.append(file_name)


def converting_song() -> dict:
    return {
        "id": "song_1",
        "userId": "usr_1",
        "status": "converting_score",
        "scoreFileName": "original.pdf",
        "sourceScoreFileName": "original.pdf",
    }


def job() -> dict:
    return {"schemaVersion": 1, "jobId": "job_1", "songId": "song_1", "userId": "usr_1"}


def write_song_json(work_dir: Path, song_id: str, payload: dict) -> None:
    """run_omr が書く曲 JSON を模擬する。"""
    songs = work_dir / "songs"
    songs.mkdir(parents=True, exist_ok=True)
    (songs / f"{song_id}.json").write_text(json.dumps(payload), encoding="utf-8")


def written_files(work_dir: Path, song_id: str, names: list[str]) -> None:
    """run_omr が生成した成果物を模擬する。"""
    score_dir = work_dir / "scores" / song_id
    score_dir.mkdir(parents=True, exist_ok=True)
    for name in names:
        (score_dir / name).write_text("x", encoding="utf-8")


class ProcessOmrJobTests(unittest.TestCase):
    def run_job(self, store, run_omr, dequeue_count=1):
        with tempfile.TemporaryDirectory() as temp:
            return process_omr_job(store, job(), dequeue_count, Path(temp), run_omr)

    def test_missing_song_is_skipped(self):
        store = FakeStore(song=None)
        outcome = self.run_job(store, lambda work_dir, song_id: 0)
        self.assertEqual(outcome, "skipped")
        self.assertEqual(store.patches, [])

    def test_song_not_converting_is_skipped(self):
        # 再配信の重複、または利用者が正しい楽譜へ差し替えて別ジョブが処理済み。
        # 完了した曲を OMR ドラフトで上書きするのは害にしかならない。
        song = converting_song()
        song["status"] = "ready"
        store = FakeStore(song)
        outcome = self.run_job(store, lambda work_dir, song_id: 0)
        self.assertEqual(outcome, "skipped")
        self.assertEqual(store.patches, [])

    def test_successful_conversion_moves_to_reviewing_score(self):
        store = FakeStore(converting_song())

        def run_omr(work_dir: Path, song_id: str) -> int:
            written_files(work_dir, song_id, ["score.musicxml", "score.mid"])
            write_song_json(work_dir, song_id, {
                "status": "reviewing_score",
                "scoreFileName": "score.musicxml",
                "scoreSource": "pdf",
                "omrEngine": "audiveris",
                "omrError": None,
                "previewScoreFileName": "score.musicxml",
                "previewMidiFileName": "score.mid",
                "warnings": [],
            })
            return 0

        outcome = self.run_job(store, run_omr)
        self.assertEqual(outcome, "completed")
        patch = store.patches[-1]
        self.assertEqual(patch["status"], "reviewing_score")
        self.assertEqual(patch["scoreFileName"], "score.musicxml")
        self.assertEqual(patch["scoreSource"], "pdf")
        self.assertIsNone(patch["omrError"])
        self.assertEqual(patch["previewMidiFileName"], "score.mid")

    def test_generated_files_are_uploaded_without_duplicates(self):
        # scoreFileName と previewScoreFileName は同一ファイルになり得る。
        # 同じ名前を2回上げない。
        store = FakeStore(converting_song())

        def run_omr(work_dir: Path, song_id: str) -> int:
            written_files(work_dir, song_id, ["score.musicxml", "score.mid"])
            write_song_json(work_dir, song_id, {
                "status": "reviewing_score",
                "scoreFileName": "score.musicxml",
                "previewScoreFileName": "score.musicxml",
                "previewMidiFileName": "score.mid",
            })
            return 0

        self.run_job(store, run_omr)
        self.assertEqual(sorted(store.uploaded), ["score.mid", "score.musicxml"])

    def test_nonzero_exit_moves_to_omr_failed(self):
        # 失敗の終端は omr_failed（api.md:278）。awaiting_score ではない。
        store = FakeStore(converting_song())

        def run_omr(work_dir: Path, song_id: str) -> int:
            write_song_json(work_dir, song_id, {
                "status": "omr_failed",
                "omrError": "Audiveris did not produce exactly one MusicXML file",
            })
            return 1

        outcome = self.run_job(store, run_omr)
        self.assertEqual(outcome, "failed")
        patch = store.patches[-1]
        self.assertEqual(patch["status"], "omr_failed")
        self.assertIn("MusicXML", patch["omrError"])
        self.assertEqual(store.uploaded, [])

    def test_status_not_reviewing_after_zero_exit_is_treated_as_failure(self):
        # 終了コード0でも曲が reviewing_score になっていないなら成功ではない。
        store = FakeStore(converting_song())

        def run_omr(work_dir: Path, song_id: str) -> int:
            write_song_json(work_dir, song_id, {"status": "omr_failed", "omrError": "boom"})
            return 0

        self.assertEqual(self.run_job(store, run_omr), "failed")
        self.assertEqual(store.patches[-1]["status"], "omr_failed")

    def test_transient_failure_below_the_cap_is_raised(self):
        store = FakeStore(converting_song())

        def run_omr(work_dir: Path, song_id: str) -> int:
            raise RuntimeError("blob unavailable")

        with self.assertRaises(RuntimeError):
            self.run_job(store, run_omr, dequeue_count=MAX_ATTEMPTS - 1)

    def test_cap_reached_terminates_as_omr_failed(self):
        store = FakeStore(converting_song())

        def run_omr(work_dir: Path, song_id: str) -> int:
            raise RuntimeError("blob unavailable")

        outcome = self.run_job(store, run_omr, dequeue_count=MAX_ATTEMPTS)
        self.assertEqual(outcome, "exhausted")
        patch = store.patches[-1]
        self.assertEqual(patch["status"], "omr_failed")
        self.assertIn("blob unavailable", patch["omrError"])

    def test_cap_reached_without_identifiers_is_skipped(self):
        # 曲を特定できないメッセージ。残しても同じ KeyError を繰り返すだけ。
        store = FakeStore(converting_song())
        with tempfile.TemporaryDirectory() as temp:
            outcome = process_omr_job(
                store, {"jobId": "job_1"}, MAX_ATTEMPTS, Path(temp),
                lambda work_dir, song_id: 0,
            )
        self.assertEqual(outcome, "skipped")
        self.assertEqual(store.patches, [])

    def test_cap_reached_but_update_fails_is_raised(self):
        # 終端状態を書けないままメッセージを消すと、曲が converting_score のまま
        # 誰も再開できない状態で孤立する。残せば Cosmos 回復後に終端できる。
        store = FakeStore(converting_song())
        store.raise_on_update = True

        def run_omr(work_dir: Path, song_id: str) -> int:
            raise RuntimeError("blob unavailable")

        with self.assertRaises(RuntimeError):
            self.run_job(store, run_omr, dequeue_count=MAX_ATTEMPTS)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
docker run --rm --entrypoint python -v "$PWD:/src" -w /src/worker ledgerlines-worker:omr -m unittest tests.test_omr_job
```

Expected: FAIL — `ModuleNotFoundError: No module named 'ledgerlines_worker.omr_job'`

- [ ] **Step 3: 実装する**

Create `worker/ledgerlines_worker/omr_job.py`:

```python
"""PDF の OMR ジョブの純ロジック（Azure SDK に依存しない）。

`cloud_worker.py` がキューの受信とストア実装（Blob / Cosmos）を担い、この
モジュールは「曲ドキュメントをどう遷移させ、どの成果物を上げ、メッセージを
削除してよいか」だけを決める。`score_job.py` と同じ方針で azure.* を import
しないので、ワーカーイメージ以外でも単体テストが動く。

OMR 自体は `worker_main.run_omr` が行う。ここでは呼ぶだけで再実装しない
（設計 §4.3）。
"""

from __future__ import annotations

import json
from pathlib import Path
from typing import Any, Callable, Protocol

# 曲が永久に converting_score のまま残るのを防ぐための試行上限。
# score_job.MAX_ATTEMPTS と同じ理由・同じ値。
MAX_ATTEMPTS = 3

# run_omr が確定させる曲ドキュメントのフィールド。
RESULT_FIELDS = (
    "scoreFileName",
    "sourceScoreFileName",
    "scoreSource",
    "omrEngine",
    "omrError",
    "lastScoreError",
    "previewScoreFileName",
    "previewMidiFileName",
    "warnings",
)


class OmrStore(Protocol):
    def get_song(self, song_id: str, user_id: str) -> dict[str, Any] | None: ...

    def update_song(self, song_id: str, user_id: str, patch: dict[str, Any]) -> Any: ...

    def download_score(self, song: dict[str, Any], target_dir: Path) -> Path: ...

    def upload_preview(self, song: dict[str, Any], file_name: str, source: Path) -> None: ...


def process_omr_job(
    store: OmrStore,
    job: dict[str, Any],
    dequeue_count: int,
    work_dir: Path,
    run_omr: Callable[[Path, str], int],
) -> str:
    """PDF を MusicXML 化し、曲を reviewing_score / omr_failed へ遷移させる。

    戻り値は呼び出し側がメッセージを削除してよいかの判断に使う。
    "completed" / "failed" / "skipped" / "exhausted" はいずれも削除してよい。
    再配信させたい失敗は例外として送出する（メッセージを残す）。
    """
    try:
        return _run_omr_job(store, job, work_dir, run_omr)
    except Exception as exc:  # noqa: BLE001
        if dequeue_count < MAX_ATTEMPTS:
            raise
        song_id = job.get("songId")
        user_id = job.get("userId")
        if not song_id or not user_id:
            # 宛先が特定できないので omrError を残せない。メッセージを残しても
            # 同じ失敗を繰り返すだけなので削除させる。利用者は曲詳細からの
            # 差し替えで先へ進める。
            return "skipped"
        # update_song 自身が投げた場合は送出させる（メッセージを残す）。終端状態を
        # 書けないまま消すと、曲が converting_score のまま孤立する。
        store.update_song(
            song_id,
            user_id,
            {"status": "omr_failed", "omrError": str(exc)},
        )
        return "exhausted"


def _run_omr_job(
    store: OmrStore,
    job: dict[str, Any],
    work_dir: Path,
    run_omr: Callable[[Path, str], int],
) -> str:
    """試行上限を意識しない本体。失敗は素通しして呼び出し側に判断させる。"""
    song_id = job["songId"]
    user_id = job["userId"]
    song = store.get_song(song_id, user_id)
    if song is None:
        return "skipped"
    if song.get("status") != "converting_score":
        # 再配信の重複か、利用者が楽譜を差し替えて別のジョブが処理済み。
        # 完了済みの曲を OMR ドラフトで上書きするのは害にしかならない。
        return "skipped"

    score_dir = _materialize_inputs(store, song, work_dir)
    code = run_omr(work_dir, song_id)
    parsed = json.loads(
        (work_dir / "songs" / f"{song_id}.json").read_text(encoding="utf-8")
    )

    if code != 0 or parsed.get("status") != "reviewing_score":
        store.update_song(
            song_id,
            user_id,
            {
                "status": "omr_failed",
                "omrError": parsed.get("omrError") or "OMR conversion failed",
            },
        )
        return "failed"

    # 変換後の楽譜とプレビューのファイル名は run_omr が決める（入力の拡張子と
    # Audiveris の出力で変わる）。決め打ちしてはいけない。同じ名前が複数の
    # フィールドに現れ得るので重複を除く。
    for file_name in dict.fromkeys(
        name
        for name in (
            parsed.get("scoreFileName"),
            parsed.get("previewScoreFileName"),
            parsed.get("previewMidiFileName"),
        )
        if name
    ):
        store.upload_preview(song, file_name, score_dir / file_name)

    store.update_song(
        song_id,
        user_id,
        {
            "status": "reviewing_score",
            **{key: parsed.get(key) for key in RESULT_FIELDS if key in parsed},
        },
    )
    return "completed"


def _materialize_inputs(store: OmrStore, song: dict[str, Any], work_dir: Path) -> Path:
    """run_omr が期待するファイルレイアウトを一時領域に用意する。

    戻り値は成果物が置かれるディレクトリ（run_omr は変換後の楽譜を PDF と同じ
    ディレクトリに書く）。
    """
    songs_dir = work_dir / "songs"
    songs_dir.mkdir(parents=True, exist_ok=True)
    (songs_dir / f"{song['id']}.json").write_text(
        json.dumps(song, ensure_ascii=False), encoding="utf-8"
    )
    score_dir = work_dir / "scores" / song["id"]
    score_dir.mkdir(parents=True, exist_ok=True)
    store.download_score(song, score_dir)
    return score_dir
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
docker run --rm --entrypoint python -v "$PWD:/src" -w /src/worker ledgerlines-worker:omr -m unittest tests.test_omr_job -v
```

Expected: 10 tests PASS

- [ ] **Step 5: 全 worker テストを流す**

```bash
docker run --rm --entrypoint python -v "$PWD:/src" -w /src/worker ledgerlines-worker:omr -m unittest discover -s tests
```

Expected: OK（既存件数 ＋ 10）

- [ ] **Step 6: Commit**

```bash
git add worker/ledgerlines_worker/omr_job.py worker/tests/test_omr_job.py
git commit -m "feat: add the OMR job state machine as pure logic"
```

---

### Task 3: ワーカーのループに OMR キューを足す

**Files:**
- Modify: `worker/cloud_worker.py`（`DEFAULT_SCORE_QUEUE` 付近、`CloudStore.__init__` のクライアント生成、`_drain_score_queue` の後、`main()`）
- Create: `worker/tests/test_cloud_worker_queues.py`

**Interfaces:**
- Consumes: `ledgerlines_worker.omr_job.process_omr_job`、`worker_main.run_omr`
- Produces: `omr_queue_name() -> str`、`_drain_omr_queue(store, visibility_seconds) -> bool`、`_drain_analysis_queue(store, visibility_seconds) -> bool`

- [ ] **Step 1: 失敗するテストを書く**

Create `worker/tests/test_cloud_worker_queues.py`。**`cloud_worker` は `azure.*` を import するため、イメージ内でのみ動く**（他の worker テストと同じくコンテナで実行する）。

```python
from __future__ import annotations

import os
import sys
import unittest
from pathlib import Path
from unittest import mock

ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT / "worker"))

import cloud_worker  # noqa: E402


class QueueNameTests(unittest.TestCase):
    def test_omr_queue_falls_back_to_the_default(self):
        # CD は `az containerapp update --image` だけを行い Bicep を流さないため、
        # AZURE_OMR_QUEUE を持たないリビジョンが動く。required() にすると
        # ワーカーが再起動ループに入り、動いている解析まで落ちる（#33 と同型）。
        with mock.patch.dict(os.environ, {}, clear=False):
            os.environ.pop("AZURE_OMR_QUEUE", None)
            self.assertEqual(cloud_worker.omr_queue_name(), "omr-jobs")

    def test_empty_omr_queue_name_falls_back_too(self):
        with mock.patch.dict(os.environ, {"AZURE_OMR_QUEUE": "   "}):
            self.assertEqual(cloud_worker.omr_queue_name(), "omr-jobs")

    def test_explicit_omr_queue_name_is_used(self):
        with mock.patch.dict(os.environ, {"AZURE_OMR_QUEUE": "omr-jobs-stg"}):
            self.assertEqual(cloud_worker.omr_queue_name(), "omr-jobs-stg")


class FakeQueue:
    def __init__(self, messages=()):
        self.messages = list(messages)
        self.deleted = []

    def receive_messages(self, messages_per_page=1, visibility_timeout=None):
        if not self.messages:
            return iter([])
        return iter([self.messages.pop(0)])

    def delete_message(self, message):
        self.deleted.append(message)


class FakeMessage:
    def __init__(self, content: str, dequeue_count: int = 1):
        self.content = content
        self.dequeue_count = dequeue_count


class DrainOmrQueueTests(unittest.TestCase):
    def test_no_messages_returns_false(self):
        store = mock.Mock()
        store.omr_queue = FakeQueue()
        self.assertFalse(cloud_worker._drain_omr_queue(store, 900))

    def test_processed_message_is_deleted_and_returns_true(self):
        store = mock.Mock()
        message = FakeMessage('{"songId": "song_1", "userId": "usr_1", "jobId": "job_1"}')
        store.omr_queue = FakeQueue([message])
        with mock.patch.object(cloud_worker, "process_omr_job", return_value="completed"):
            self.assertTrue(cloud_worker._drain_omr_queue(store, 900))
        self.assertEqual(store.omr_queue.deleted, [message])

    def test_raised_failure_leaves_the_message(self):
        # process_omr_job は再試行に意味がある失敗だけを送出する。
        store = mock.Mock()
        message = FakeMessage('{"songId": "song_1", "userId": "usr_1"}')
        store.omr_queue = FakeQueue([message])
        with mock.patch.object(cloud_worker, "process_omr_job", side_effect=RuntimeError("boom")):
            self.assertTrue(cloud_worker._drain_omr_queue(store, 900))
        self.assertEqual(store.omr_queue.deleted, [])

    def test_broken_json_does_not_escape(self):
        # songId が読めないメッセージでも例外を外に出さない（ループを止めない）。
        store = mock.Mock()
        store.omr_queue = FakeQueue([FakeMessage("not json")])
        self.assertTrue(cloud_worker._drain_omr_queue(store, 900))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
docker run --rm --entrypoint python -v "$PWD:/src" -w /src/worker ledgerlines-worker:omr -m unittest tests.test_cloud_worker_queues
```

Expected: FAIL — `AttributeError: module 'cloud_worker' has no attribute 'omr_queue_name'`

- [ ] **Step 3: キュー名とクライアントを足す**

`DEFAULT_SCORE_QUEUE = "score-jobs"` の直後に足す:

```python
DEFAULT_OMR_QUEUE = "omr-jobs"
```

`score_queue_name()` の直後に足す:

```python
def omr_queue_name() -> str:
    """OMR ジョブのキュー名。

    score_queue_name() と同じ理由で required() にしない。CD は
    `az containerapp update --image` だけを行い Bicep を流さないため、
    AZURE_OMR_QUEUE を持たないリビジョンが動く。そこで落とすと再起動ループに
    入り、動いている解析パイプラインまで止まる。
    """
    return os.environ.get("AZURE_OMR_QUEUE", "").strip() or DEFAULT_OMR_QUEUE
```

`CloudStore.__init__` の `self.score_queue = ...` を作っている2箇所（接続文字列版と資格情報版）の直後に、同じ形で `self.omr_queue` を作る。**`score_queue` の生成方法をそのまま写し、キュー名だけ `omr_queue_name()` にする。**

- [ ] **Step 4: `_drain_omr_queue` を足す**

`_drain_score_queue` の直後に置く。**`_drain_score_queue` の構造をそのまま写す**（例外を外に出さない、`json.loads` の失敗も含めて `process_omr_job` に判断を委ねる形）。

```python
def _drain_omr_queue(store: CloudStore, visibility_seconds: int) -> bool:
    """omr-jobs を1件処理する。処理したら True（呼び出し側が先頭から見直す）。"""
    messages = list(
        store.omr_queue.receive_messages(messages_per_page=1, visibility_timeout=visibility_seconds)
    )
    if not messages:
        return False
    for message in messages:
        try:
            job = json.loads(message.content)
            with tempfile.TemporaryDirectory(prefix=f"ledgerlines-omr-{job.get('songId', 'unknown')}-") as temp:
                outcome = process_omr_job(
                    store, job, message.dequeue_count, Path(temp), run_omr
                )
            store.omr_queue.delete_message(message)
            LOGGER.info("OMR job %s outcome=%s song=%s", job.get("jobId"), outcome, job.get("songId"))
        except Exception:
            # process_omr_job は再試行に意味がある失敗だけを送出する
            # （MAX_ATTEMPTS 到達時は自分で終端させて戻る）。
            LOGGER.exception("OMR job failed; leaving message for retry (max %s attempts)", OMR_MAX_ATTEMPTS)
    return True
```

import を追加する（既存の `from ledgerlines_worker.score_job import MAX_ATTEMPTS, process_score_job` に相当する行の近く）:

```python
from ledgerlines_worker.omr_job import MAX_ATTEMPTS as OMR_MAX_ATTEMPTS, process_omr_job
from worker_main import run_omr
```

**注意**: `run_reference` が既にどう import されているかを確認し、同じ形にすること。`run_omr` も `worker_main` にある。

- [ ] **Step 5: `main()` の優先順位を組み替える**

現在の解析分岐を真偽を返すヘルパーへ抽出し、3本のキューの優先順位を1箇所で読めるようにする。**挙動を変えないこと**（解析の処理内容・ログ・例外の扱いはそのまま移す）。

```python
def _drain_analysis_queue(store: CloudStore, visibility_seconds: int) -> bool:
    """analysis-jobs を1件処理する。処理したら True。"""
    messages = list(store.queue.receive_messages(messages_per_page=1, visibility_timeout=visibility_seconds))
    if not messages:
        return False
    for message in messages:
        try:
            job = json.loads(message.content)
            process_job(store, job)
            store.queue.delete_message(message)
            LOGGER.info("Completed analysis job %s", job.get("jobId"))
        except Exception:
            LOGGER.exception("Analysis job failed; leaving message for retry")
    return True


def main() -> None:
    store = CloudStore()
    polling_seconds = int(os.environ.get("WORKER_POLLING_SECONDS", "5"))
    visibility_seconds = int(os.environ.get("WORKER_VISIBILITY_TIMEOUT_SECONDS", "1800"))
    score_visibility_seconds = int(os.environ.get("WORKER_SCORE_VISIBILITY_TIMEOUT_SECONDS", "300"))
    # Audiveris は既定300秒（AUDIVERIS_TIMEOUT_SECONDS）。ダウンロード・
    # アップロード・プレビュー生成の余裕を足す。
    omr_visibility_seconds = int(os.environ.get("WORKER_OMR_VISIBILITY_TIMEOUT_SECONDS", "900"))
    LOGGER.info("Analysis worker started")
    while True:
        # 優先順位: 参照譜生成（数秒、利用者が待っている） > 演奏分析（数分、
        # このアプリの中心価値） > OMR（数分、プレビューの下書き）。
        # OMR を最後に置くのは、下書きの生成で採点を遅らせないため。レプリカ1・
        # ループ1本という制約は設計 §4.2 の既知の制約。
        if _drain_score_queue(store, score_visibility_seconds):
            continue
        if _drain_analysis_queue(store, visibility_seconds):
            continue
        if _drain_omr_queue(store, omr_visibility_seconds):
            continue
        time.sleep(polling_seconds)
```

- [ ] **Step 6: テストが通ることを確認する**

```bash
docker run --rm --entrypoint python -v "$PWD:/src" -w /src/worker ledgerlines-worker:omr -m unittest tests.test_cloud_worker_queues -v
docker run --rm --entrypoint python -v "$PWD:/src" -w /src/worker ledgerlines-worker:omr -m unittest discover -s tests
```

Expected: 7 tests PASS / 全体 OK

- [ ] **Step 7: Commit**

```bash
git add worker/cloud_worker.py worker/tests/test_cloud_worker_queues.py
git commit -m "feat: poll an omr-jobs queue after score and analysis work"
```

---

### Task 4: Web から OMR ジョブを投入する

**Files:**
- Modify: `src/lib/server/config.ts`（`scoreQueueName` の隣）
- Modify: `src/lib/server/queue.ts`（`ScoreJob` 以下の塊の直後）
- Modify: `src/app/api/songs/[songId]/score/route.ts:52-53`
- Create: `src/lib/server/omr-queue.test.ts`
- Modify: `package.json`（`test:unit` に追加）

**Interfaces:**
- Consumes: `getConfig().omrQueueName`
- Produces: `OmrJob { schemaVersion: 1; jobId: string; songId: string; userId: string; attempt: number; correlationId: string }`、`OmrQueue`、`LocalOmrQueue`、`AzureOmrQueue`、`getOmrQueue()`、`resetOmrQueueForTests()`

- [ ] **Step 1: 失敗するテストを書く**

Create `src/lib/server/omr-queue.test.ts`。**`config.ts` は環境変数を読むため、既定値の解決だけを対象にする**（Azure クライアントは生成しない）。

```typescript
import assert from "node:assert/strict";
import test from "node:test";

import { getConfig, resetConfigForTests } from "./config";

function withEnv(env: Record<string, string | undefined>, run: () => void): void {
  const saved = new Map<string, string | undefined>();
  for (const key of Object.keys(env)) {
    saved.set(key, process.env[key]);
    if (env[key] === undefined) delete process.env[key];
    else process.env[key] = env[key];
  }
  resetConfigForTests();
  try {
    run();
  } finally {
    for (const [key, value] of saved) {
      if (value === undefined) delete process.env[key];
      else process.env[key] = value;
    }
    resetConfigForTests();
  }
}

test("OMR キュー名は未設定なら omr-jobs", () => {
  // ワーカー側（cloud_worker.omr_queue_name）と同じ既定値。片方だけ変えると
  // Web が投入したジョブを誰も読まない状態になる。
  withEnv({ AZURE_OMR_QUEUE: undefined }, () => {
    assert.equal(getConfig().omrQueueName, "omr-jobs");
  });
});

test("OMR キュー名が空文字でも omr-jobs にフォールバックする", () => {
  withEnv({ AZURE_OMR_QUEUE: "   " }, () => {
    assert.equal(getConfig().omrQueueName, "omr-jobs");
  });
});

test("OMR キュー名は明示されればそれを使う", () => {
  withEnv({ AZURE_OMR_QUEUE: "omr-jobs-stg" }, () => {
    assert.equal(getConfig().omrQueueName, "omr-jobs-stg");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
npx tsx --test src/lib/server/omr-queue.test.ts
```

Expected: FAIL — `omrQueueName` が `undefined`

- [ ] **Step 3: config に足す**

`AppConfig` に `omrQueueName: string;` を足す（`scoreQueueName` は `:39`）。値の解決は `scoreQueueName`（`:140`）の**隣に置くが、書き方は写さない**。

```typescript
    // 空文字も既定値へ落とす。ワーカー側（cloud_worker.omr_queue_name）と挙動を
    // 揃えるため——片方だけが "" を受け入れると、Web が名前の無いキューへ投げて
    // ワーカーは omr-jobs を読む、という食い違いになる。
    omrQueueName: process.env.AZURE_OMR_QUEUE?.trim() || "omr-jobs",
```

**既存の `scoreQueueName` は `process.env.AZURE_SCORE_QUEUE ?? "score-jobs"` で、
空文字のときフォールバックしない**（`??` は `""` を値として通す）。ワーカー側は
`.strip() or DEFAULT_SCORE_QUEUE` なのでフォールバックする。この非対称は #45 の
スコープ外なので**このタスクでは直さない**が、**Task 7 の結果文書に follow-up 候補
として記録する**。

- [ ] **Step 4: テストが通ることを確認する**

```bash
npx tsx --test src/lib/server/omr-queue.test.ts
```

Expected: 3 tests PASS

- [ ] **Step 5: キューを足す**

`src/lib/server/queue.ts` の `resetScoreQueueForTests()` の直後に、`ScoreJob` 以下の塊と**同じ形**で足す。

```typescript
export interface OmrJob {
  schemaVersion: 1;
  jobId: string;
  songId: string;
  userId: string;
  attempt: number;
  correlationId: string;
}

export interface OmrQueue {
  enqueue(job: OmrJob): Promise<void>;
}

export class LocalOmrQueue implements OmrQueue {
  async enqueue(job: OmrJob): Promise<void> {
    // ローカルバックエンドは従来どおり同期実行（route が結果を返す）。ここは
    // azure バックエンドと契約を揃えるためのラッパーで、spawn の実装は
    // worker.ts の内側に閉じる。
    const { runOmrWorker } = await import("./worker");
    await runOmrWorker(job.songId);
    getTelemetry().record({ name: "omr.queue.enqueued", jobId: job.jobId, songId: job.songId, stage: "local" });
  }
}

export class AzureOmrQueue implements OmrQueue {
  private readonly client: QueueClient;

  constructor() {
    const config = getConfig();
    this.client = config.azureEmulator
      ? QueueServiceClient.fromConnectionString(config.storageConnectionString!).getQueueClient(config.omrQueueName)
      : new QueueClient(
          `${config.storageQueueUrl ?? config.storageAccountUrl}/${config.omrQueueName}`,
          createAzureCredential()
        );
  }

  async enqueue(job: OmrJob): Promise<void> {
    // 解析・参照譜生成と同じ約束: メッセージは識別子だけを載せる。
    await this.client.sendMessage(JSON.stringify(job));
    getTelemetry().record({ name: "omr.queue.enqueued", jobId: job.jobId, songId: job.songId, stage: "azure" });
  }
}

let omrQueue: OmrQueue | undefined;
export function getOmrQueue(): OmrQueue {
  omrQueue ??= getConfig().queueBackend === "azure" ? new AzureOmrQueue() : new LocalOmrQueue();
  return omrQueue;
}

export function resetOmrQueueForTests(): void {
  omrQueue = undefined;
}
```

- [ ] **Step 6: route から投入する**

`src/app/api/songs/[songId]/score/route.ts` の PDF 分岐で、azure バックエンドのとき **202 を返す前に enqueue する**。`jobId` / `correlationId` の作り方は同ファイルの `score-jobs` 投入（`:73-79`）と揃える。

```typescript
      if (getConfig().storageBackend === "azure") {
        // 以前はここで 202 を返して終わっており、OMR を実行する主体がいなかった
        // ため曲が converting_score のまま留まっていた（#45）。
        await getOmrQueue().enqueue({
          schemaVersion: 1,
          jobId: randomUUID(),
          songId,
          userId: user.id,
          attempt: 1,
          correlationId: request.headers.get("x-request-id") ?? randomUUID(),
        });
        return jsonResponse({ songId, status: "converting_score", uploadComplete: true }, request, { status: 202 });
      }
```

`randomUUID` と `getOmrQueue` の import を足す（`randomUUID` は `:73-79` の経路で既に import 済みのはず。確認して重複させない）。

- [ ] **Step 7: `test:unit` に登録して全体を流す**

`package.json` の `test:unit` の一覧に `src/lib/server/omr-queue.test.ts` を追加する。

```bash
npm run test:unit
npx tsc --noEmit
npm run lint
npm run build
```

Expected: すべて成功

- [ ] **Step 8: Commit**

```bash
git add src/lib/server/config.ts src/lib/server/queue.ts "src/app/api/songs/[songId]/score/route.ts" src/lib/server/omr-queue.test.ts package.json
git commit -m "feat: enqueue an OMR job so deployed PDF uploads get converted"
```

---

### Task 5: IaC にキューを足し、適用順序を runbook に書く

**Files:**
- Modify: `infra/main.bicep`（`queues` の配列）
- Modify: `infra/modules/analysis-worker.bicep`（`AZURE_SCORE_QUEUE` を渡している箇所の隣）
- Modify: `docs/operations/iac-runbook.md`（§5.1）

**Interfaces:**
- Consumes: Task 3 の `AZURE_OMR_QUEUE`、Task 4 の `omrQueueName`

- [ ] **Step 1: 現状を確認する**

```bash
grep -n "score-jobs" infra/main.bicep infra/modules/analysis-worker.bicep
```

`score-jobs` がどう宣言され、どうワーカーへ渡っているかを読む。**同じ形で `omr-jobs` を足す**。

- [ ] **Step 2: キューを足す**

`infra/main.bicep` の `queues` に `omr-jobs` を、`score-jobs` と同じ形で追加する。

- [ ] **Step 3: ワーカーへ環境変数を渡す**

`infra/modules/analysis-worker.bicep` で `AZURE_SCORE_QUEUE` を渡している形をそのまま写して `AZURE_OMR_QUEUE` を渡す。

- [ ] **Step 4: 検証する**

```bash
az bicep build --file infra/main.bicep --stdout > /dev/null && echo "bicep OK"
npm run test:infra
```

Expected: `bicep OK` / infra テスト成功

**`az` が使えない場合**は `npm run test:infra` と目視確認に留め、**Bicep のビルド検証をしていないことを報告に書く**。

- [ ] **Step 5: runbook に適用順序を書く**

`docs/operations/iac-runbook.md` §5.1 の「参照譜生成のキュー化（Issue #33）は次を要求します」に並べて、OMR のキュー化を追記する。

```markdown
PDFのOMRのキュー化（Issue #45）は次を要求します。

- `omr-jobs` Storage Queue（`infra/main.bicep` の `queues`）
  未作成のままWebをデプロイすると、PDFアップロードのenqueueが `QueueNotFound` で
  失敗し、`POST /api/songs/{songId}/score` がPDFに対して500を返します。
  MusicXML / MXL / MIDI の登録は影響を受けません。
- ワーカーの `AZURE_OMR_QUEUE` 環境変数（`infra/modules/analysis-worker.bicep`）
  ワーカーは未設定時に既定値 `omr-jobs` へフォールバックするため起動はしますが、
  キュー名を宣言で管理するため、IaCを流して明示設定へ揃えます。
```

- [ ] **Step 6: Commit**

```bash
git add infra/main.bicep infra/modules/analysis-worker.bicep docs/operations/iac-runbook.md
git commit -m "feat: declare the omr-jobs queue and document its deploy ordering"
```

---

### Task 6: 変換中に止まった曲を復旧できるようにし、仕様の食い違いを直す

**Files:**
- Modify: `src/app/songs/[id]/page.tsx`（`scoreReplacementReason`）
- Modify: `src/components/VerifiedScoreReplacement.tsx`（`ScoreReplacementReason` と表示文）
- Modify: `docs/spec/api.md:278`

**Interfaces:**
- Produces: `ScoreReplacementReason` に `"converting"` を追加

- [ ] **Step 1: 現状を確認する**

```bash
grep -n "ScoreReplacementReason" -A 20 src/components/VerifiedScoreReplacement.tsx | head -30
```

既存の理由（`pdf_draft` / `parse_failed` / `not_uploaded` / `parsing`）とそれぞれの表示文を読む。

- [ ] **Step 2: `converting` を足す**

`ScoreReplacementReason` の union に `"converting"` を足し、他の理由と同じ形で表示文を書く。文面の趣旨:

> PDFをMusicXMLへ変換中です。変換は自動で進みますが、待たずに正しいMusicXML、MXL、またはMIDIへ差し替えることもできます。

- [ ] **Step 3: `scoreReplacementReason` が `converting_score` を返すようにする**

```typescript
  if (song.status === "parsing_score") return "parsing";
  if (song.status === "converting_score") return "converting";
  return null;
```

**理由をコメントに残す**: 変換が止まった曲に差し替え導線が無いと、削除以外に復旧手段が無い（#45）。

- [ ] **Step 4: 仕様の食い違いを直す**

`docs/spec/api.md:278` の status 表の `reviewing_score` の説明を、同じ節の本文（`:262-264`）と一致させる。

```markdown
| `reviewing_score` | OMRが生成した比較用ドラフト。分析には使用しない終端状態で、利用者は正しいMusicXML、MXL、またはMIDIへ差し替えて分析を開始する |
```

**「承認するまで待機」という表現を使わないこと。** 承認して採用する経路は存在しない（`POST /songs/{songId}/score/approve` は案内文を返すだけで、クライアントからの呼び出しも無い）。

- [ ] **Step 5: 検証する**

```bash
npx tsc --noEmit
npm run lint
npm run build
```

Expected: すべて成功

- [ ] **Step 6: Commit**

```bash
git add "src/app/songs/[id]/page.tsx" src/components/VerifiedScoreReplacement.tsx docs/spec/api.md
git commit -m "fix: offer a way out of converting_score and align the spec with it"
```

---

### Task 7: 失敗経路をエミュレータで実走させ、結果を記録する

**Files:**
- Create: `docs/superpowers/plans/2026-08-17-omr-queue-results.md`

**Interfaces:**
- Consumes: Task 1〜6 のすべて

- [ ] **Step 1: 楽譜でない最小の PDF を用意する**

`%PDF-` 署名を持つ有効な最小 PDF を作る（`route.ts` の署名検証を通過させるため）。スクラッチ領域に作り、**リポジトリにはコミットしない**。

```bash
printf '%%PDF-1.4\n1 0 obj<</Type/Catalog/Pages 2 0 R>>endobj\n2 0 obj<</Type/Pages/Kids[3 0 R]/Count 1>>endobj\n3 0 obj<</Type/Page/Parent 2 0 R/MediaBox[0 0 200 200]>>endobj\ntrailer<</Root 1 0 R>>\n%%%%EOF\n' > "$SCRATCH/not-a-score.pdf"
```

- [ ] **Step 2: エミュレータを起動してキューを作る**

```bash
npm run azure:up
npm run azure:init
```

`omr-jobs` キューが作られることを確認する。**作られない場合は `scripts/azure-local.ts` の init がキュー一覧をどこから読んでいるかを確認し、`omr-jobs` を足す**（足したらそれもコミット対象）。

- [ ] **Step 3: この PDF をアップロードして終端を確認する**

曲を作成し、`POST /api/songs/{songId}/score` に上記 PDF を送る。ワーカーをエミュレータプロファイルで動かし、曲が **`omr_failed`** に落ち `omrError` が入ることを確認する。

確認すべきこと:
- 曲が `converting_score` → `omr_failed` へ遷移する（永久に `converting_score` に留まらない）
- `omrError` に Audiveris の失敗理由が入る
- SSE が `omr_failed` を終端として扱い、10分待たずに終わる

- [ ] **Step 4: 結果を記録する**

`docs/superpowers/plans/2026-08-17-omr-queue-results.md` に次を書く。

- Task 1 で測ったイメージサイズの増分（`omr-before` と `omr` の実測値）
- Audiveris の起動確認の出力
- 失敗経路の実走結果（状態遷移と `omrError` の実際の文言）
- **成功経路（実際の楽譜 PDF の変換）は未検証であること。** 理由（リポジトリに楽譜 PDF が無く、MusicXML から PDF を作る手段がこの環境に無い）と、楽譜 PDF が提供されれば同じ手順で確認できることを明記する
- Task 1 で配布アーカイブの想定が違っていた場合は、何をどう変えたか
- **follow-up 候補**: `config.ts:140` の `scoreQueueName` は `process.env.AZURE_SCORE_QUEUE ?? "score-jobs"` で、空文字のときフォールバックしない。ワーカー側（`cloud_worker.score_queue_name`）は `.strip() or` でフォールバックするため、`AZURE_SCORE_QUEUE=""` のとき Web は名前の無いキューへ投げてワーカーは `score-jobs` を読む食い違いになる。#45 のスコープ外として直していない

**測っていないことを検証済みとして書かないこと。**

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/plans/2026-08-17-omr-queue-results.md
git commit -m "docs: record what the OMR queue verification covered and what it did not"
```

---

## Self-Review

**1. Spec coverage**

| 設計の節 | 対応するタスク |
|---|---|
| §4.1 キューを分ける（`omr-jobs`、空文字フォールバック） | Task 3（ワーカー側）、Task 4（Web 側）、Task 5（IaC・runbook） |
| §4.2 ワーカーのループと優先順位 | Task 3 Step 5 |
| §4.3 OMR ジョブの純ロジック | Task 2 |
| §4.4 Web 側の enqueue | Task 4 Step 6 |
| §4.4 `converting_score` の差し替え導線 | Task 6 |
| §4.5 ライセンスの追跡 | Task 1 Step 6 |
| §2 仕様の食い違い（`api.md:278`） | Task 6 Step 4 |
| §5.1 失敗経路の実走 | Task 7 |
| §5.2 成功経路が未検証であることの明記 | Task 7 Step 4 |
| §3 イメージサイズの増分の記録 | Task 1 Step 5、Task 7 Step 4 |

ギャップなし。

**2. Placeholder scan**

初稿では Task 4 Step 3 と Step 6 に「既存の書き方に揃える」という指示だけを置いていた。実物を確認して両方を実コードに置き換えた（`randomUUID()` / `request.headers.get("x-request-id") ?? randomUUID()`、`process.env.AZURE_OMR_QUEUE?.trim() || "omr-jobs"`）。**特に Step 3 は「`scoreQueueName` と同じ形にする」と書いていたが、それは `??` で空文字を通すため、写すと Global Constraints の「空文字で既定値へフォールバック」に反していた。** 実コードを書き、既存の非対称は follow-up として記録する形に改めた。

Task 5 は `score-jobs` の宣言を読んで同じ形で足す指示にしている（Bicep の該当箇所の現物を実装者が読む必要があり、行の内容はリポジトリにある）。Task 6 Step 2 の表示文は文面の趣旨を与えている。それ以外に TBD・「適切に」等の記述は無い。

**3. Type consistency**

- `process_omr_job(store, job, dequeue_count, work_dir, run_omr)` — Task 2 で定義、Task 3 Step 4 で同じ引数順で呼ぶ
- `MAX_ATTEMPTS` — Task 2 で定義、Task 3 では `OMR_MAX_ATTEMPTS` として別名 import（`score_job.MAX_ATTEMPTS` と衝突するため）
- `omr_queue_name()` / `_drain_omr_queue` / `_drain_analysis_queue` — Task 3 で定義、テストも同名を使う
- `OmrJob` の6フィールド — Task 4 Step 5 で定義、Step 6 の enqueue が同じ6つを渡す
- `omrQueueName` — Task 4 Step 3 で定義、Step 5 の両クライアントが使う
- `ScoreReplacementReason` の `"converting"` — Task 6 Step 2 で追加、Step 3 で返す
- `upload_preview` — Task 2 の `OmrStore` が要求し、`cloud_worker.CloudStore` に既存（`:214`）
